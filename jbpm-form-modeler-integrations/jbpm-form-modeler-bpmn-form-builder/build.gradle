
description = ''
dependencies {
  compile project(':jbpm-form-modeler-core:jbpm-form-modeler-api')
    compile group: 'org.eclipse', name: 'org.eclipse.bpmn2', version:'0.7.3'
    compile group: 'org.jbpm', name: 'jbpm-bpmn2-emfextmodel', version:'6.1.0.Final'
    compile group: 'commons-lang', name: 'commons-lang', version:'2.6'
    compile group: 'org.uberfire', name: 'uberfire-backend-server', version:'0.4.0.Final'
    compile(group: 'javax.inject', name: 'javax.inject', version:'1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}

task packageTests(type: Jar) {
  from sourceSets.test.output
  classifier = 'tests'
}
artifacts.archives packageTests
